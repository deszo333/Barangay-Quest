package com.example.barangay_quest_flutter

import io.flutter.embedding.android.FlutterActivity
// We are removing the FlutterEngine import and the configureFlutterEngine function

class MainActivity: FlutterActivity() {
    // That's it. No extra code.
}